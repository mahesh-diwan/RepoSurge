# Astro Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate RepoSurge from Next.js 14 to Astro 5.x with polished brutalist design, SSG-only, no React.

**Architecture:** Pure Astro components (`.astro`) with vanilla `<script>` tags for interactivity. Data fetched at build time via `scripts/fetch-repos.ts` → `src/content/repos.json`. Tailwind CSS for styling with custom design tokens.

**Tech Stack:** Astro 5.x, TypeScript, Tailwind CSS 3.4, vanilla JS

## Global Constraints

- Design tokens: `#0066FF` (electric), `#0A0A0A` (midnight), `#F5F5F0` (bone)
- Monospace font: `font-family: 'JetBrains Mono', ui-monospace, monospace`
- Zero border-radius everywhere
- No React, no Alpine.js — vanilla `<script>` only
- SSG only — no server-side rendering
- Existing data: `data/repos.json` structure preserved, fetch script adapted

---

## File Structure

```
src/
├── content/repos.json              (generated by scripts/fetch-repos.ts)
├── layouts/Base.astro              (HTML shell + nav + footer)
├── pages/
│   ├── index.astro                 (home: hero + rankings)
│   ├── repo/[slug].astro           (detail: stats + full chart)
│   └── about.astro                 (methodology)
├── components/
│   ├── Header.astro                (hero section)
│   ├── RepoCard.astro              (single repo row)
│   ├── StarChart.astro             (CSS bar sparkline)
│   ├── FilterBar.astro             (time tabs + language filter)
│   ├── SearchBar.astro             (client-side search)
│   ├── SortSelect.astro            (sort dropdown)
│   └── TrustLogos.astro            (logo strip)
├── styles/
│   └── global.css                  (design tokens + base styles)
└── data/
    └── repos.json                  (copied from root data/)
scripts/
└── fetch-repos.ts                  (adapted from current script)
```

---

### Task 1: Initialize Astro project

**Files:**

- Create: `astro.config.mjs`
- Create: `tsconfig.json`
- Create: `package.json` (overwrite)
- Create: `tailwind.config.mjs`
- Create: `postcss.config.js`

**Steps:**

- [ ] **Step 1: Remove Next.js files**

```bash
rm -rf app/ components/ lib/ src/ next-env.d.ts next.config.js .next/
```

- [ ] **Step 2: Create package.json**

```json
{
  "name": "reposurge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch": "tsx scripts/fetch-repos.ts"
  },
  "dependencies": {
    "astro": "^5.12.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.0",
    "@types/node": "^20.14.0",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
```

- [ ] **Step 3: Install dependencies**

```bash
npm install
```

- [ ] **Step 4: Create astro.config.mjs**

```js
import { defineConfig } from "astro/config";
import tailwind from "@astrojs/tailwind";

export default defineConfig({
  integrations: [tailwind()],
});
```

- [ ] **Step 5: Create tailwind.config.mjs**

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}"],
  theme: {
    extend: {
      colors: {
        electric: "#0066FF",
        midnight: "#0A0A0A",
        bone: "#F5F5F0",
      },
      fontFamily: {
        mono: ["'JetBrains Mono'", "ui-monospace", "monospace"],
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 6: Create postcss.config.js**

```js
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

- [ ] **Step 7: Create tsconfig.json**

```json
{
  "extends": "astro/tsconfigs/strict"
}
```

- [ ] **Step 8: Verify build scaffolding**

```bash
npx astro build 2>&1 | head -5
```

Expected: Build succeeds (no pages yet, but no config errors).

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "chore: initialize Astro project, remove Next.js"
```

---

### Task 2: Design tokens + global styles

**Files:**

- Create: `src/styles/global.css`

**Steps:**

- [ ] **Step 1: Create global.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    background: #0a0a0a;
    color: #f5f5f0;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    -webkit-font-smoothing: antialiased;
  }

  * {
    border-radius: 0 !important;
  }

  ::selection {
    background: #0066ff;
    color: #0a0a0a;
  }

  :focus-visible {
    outline: 2px solid #0066ff;
    outline-offset: 2px;
  }

  a {
    text-decoration: none;
    color: inherit;
  }
}
```

- [ ] **Step 2: Verify styles load**

Add temporary `src/pages/index.astro`:

```astro
---
---
<html>
  <head><link rel="stylesheet" href="/styles/global.css"></head>
  <body><h1 class="text-electric">Test</h1></body>
</html>
```

Run: `npx astro build`
Expected: Build succeeds, output `dist/index.html` contains the h1.

- [ ] **Step 3: Commit**

```bash
git add src/styles/
git commit -m "feat: add design tokens and global styles"
```

---

### Task 3: Base layout

**Files:**

- Create: `src/layouts/Base.astro`

**Interfaces:**

- Props: `{ title: string; description?: string }`

**Steps:**

- [ ] **Step 1: Create Base.astro**

```astro
---
interface Props {
  title: string;
  description?: string;
}

const { title, description = 'repos rising. fast. ranked by star velocity.' } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content={description}>
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body class="min-h-screen">
    <nav class="border-b border-bone/20 sticky top-0 z-40 bg-midnight/95 backdrop-blur-sm">
      <div class="max-w-7xl mx-auto px-4 h-16 flex items-center justify-between">
        <a href="/" class="text-xl font-bold tracking-tight text-bone hover:text-electric transition-colors">
          REP<span class="text-electric">Ø</span>SURGE
        </a>
        <div class="hidden md:flex items-center gap-6">
          <a href="/#repos" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">Rankings</a>
          <a href="/about" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">About</a>
          <a href="https://github.com" target="_blank" rel="noreferrer" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">GitHub</a>
        </div>
        <button id="mobile-toggle" class="md:hidden text-bone hover:text-electric transition-colors" aria-label="Toggle menu">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" /></svg>
        </button>
      </div>
      <div id="mobile-menu" class="hidden md:hidden py-4 border-t border-bone/20">
        <nav class="flex flex-col gap-4 px-4">
          <a href="/#repos" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">Rankings</a>
          <a href="/about" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">About</a>
          <a href="https://github.com" target="_blank" rel="noreferrer" class="text-bone/70 text-sm tracking-widest hover:text-electric transition-colors">GitHub</a>
        </nav>
      </div>
    </nav>

    <slot />

    <footer class="mt-10 text-bone/30 text-xs border-t border-bone/20 pt-4 pb-8">
      <div class="max-w-7xl mx-auto px-4">
        data: github api | refreshed daily
      </div>
    </footer>

    <script>
      const toggle = document.getElementById('mobile-toggle');
      const menu = document.getElementById('mobile-menu');
      toggle?.addEventListener('click', () => menu?.classList.toggle('hidden'));
    </script>
  </body>
</html>
```

- [ ] **Step 2: Verify layout renders**

Update `src/pages/index.astro`:

```astro
---
import Base from '../layouts/Base.astro';
---
<Base title="RepoSurge">
  <main class="max-w-7xl mx-auto px-4">
    <h1 class="text-electric text-4xl">Test</h1>
  </main>
</Base>
```

Run: `npx astro build`
Expected: Build succeeds, `dist/index.html` has nav + footer.

- [ ] **Step 3: Commit**

```bash
git add src/layouts/
git commit -m "feat: add base layout with nav and footer"
```

---

### Task 4: Data layer

**Files:**

- Create: `scripts/fetch-repos.ts` (overwrite existing)
- Create: `src/content/repos.json` (generated)

**Interfaces:**

- Produces: `src/content/repos.json` with shape `{ repos: RepoRecord[] }`
- Each `RepoRecord`: `{ full_name, name, owner, description, language, url, stars, fetched_at, history: { stars, recorded_at }[] }`

**Steps:**

- [ ] **Step 1: Create scripts/fetch-repos.ts**

```ts
import fs from "fs";
import path from "path";

const GITHUB_API = "https://api.github";
const OUTPUT = path.join(process.cwd(), "src", "content", "repos.json");
const DATA = path.join(process.cwd(), "data", "repos.json");

const LANGUAGES = ["javascript", "python", "rust", "go", "typescript", "java"];
const REPOS_PER_LANGUAGE = 100;

type GitHubRepo = {
  id: number;
  full_name: string;
  name: string;
  owner: { login: string };
  description: string | null;
  language: string | null;
  html_url: string;
  stargazers_count: number;
};

type HistoryRow = { stars: number; recorded_at: string };
type RepoRecord = {
  full_name: string;
  name: string;
  owner: string;
  description: string | null;
  language: string | null;
  url: string;
  stars: number;
  fetched_at: string;
  history: HistoryRow[];
};

function githubFetch(path: string): Promise<any> {
  const headers: Record<string, string> = {
    Accept: "application/vnd.github+json",
    "User-Agent": "RepoSurge",
  };
  const token = process.env.GITHUB_TOKEN;
  if (token) headers.Authorization = `Bearer ${token}`;
  return fetch(`${GITHUB_API}${path}`, { headers }).then((res) => {
    if (!res.ok) throw new Error(`GitHub API ${res.status} for ${path}`);
    return res.json();
  });
}

async function fetchTopRepos(
  language: string,
  count = 100,
): Promise<GitHubRepo[]> {
  const q = encodeURIComponent(`language:${language} stars:>1`);
  const repos: GitHubRepo[] = [];
  const perPage = 100;
  const pages = Math.ceil(count / perPage);
  for (let page = 1; page <= pages; page++) {
    const data = await githubFetch(
      `/search/repositories?q=${q}&sort=stars&order=desc&per_page=${perPage}&page=${page}`,
    );
    repos.push(...data.items);
    if (data.items.length < perPage) break;
  }
  return repos.slice(0, count);
}

function readExisting(): { repos: RepoRecord[] } {
  try {
    return JSON.parse(fs.readFileSync(OUTPUT, "utf8"));
  } catch {
    return { repos: [] };
  }
}

function readOldData(): { repos: RepoRecord[] } {
  try {
    return JSON.parse(fs.readFileSync(DATA, "utf8"));
  } catch {
    return { repos: [] };
  }
}

async function main() {
  const today = new Date().toISOString();
  const store = readExisting();
  const oldStore = readOldData();

  for (const language of LANGUAGES) {
    console.log(`Fetching top ${REPOS_PER_LANGUAGE} ${language} repos...`);
    try {
      const repos = await fetchTopRepos(language, REPOS_PER_LANGUAGE);
      for (const repo of repos) {
        const existing = store.repos.find(
          (r) => r.full_name === repo.full_name,
        );
        const old = oldStore.repos.find((r) => r.full_name === repo.full_name);

        if (existing) {
          existing.stars = repo.stargazers_count;
          existing.fetched_at = today;
          existing.history.push({
            stars: repo.stargazers_count,
            recorded_at: today,
          });
        } else {
          const history: HistoryRow[] = old?.history ?? [];
          history.push({ stars: repo.stargazers_count, recorded_at: today });
          store.repos.push({
            full_name: repo.full_name,
            name: repo.name,
            owner: repo.owner.login,
            description: repo.description,
            language: repo.language,
            url: repo.html_url,
            stars: repo.stargazers_count,
            fetched_at: today,
            history,
          });
        }
      }
      console.log(`  saved ${repos.length} ${language} repos`);
    } catch (err) {
      console.error(`  failed ${language}:`, (err as Error).message);
    }
  }

  fs.mkdirSync(path.dirname(OUTPUT), { recursive: true });
  fs.writeFileSync(OUTPUT, JSON.stringify(store, null, 2));
  console.log(`Done. Wrote ${store.repos.length} repos to ${OUTPUT}`);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: Copy existing data to src/content/**

```bash
mkdir -p src/content
cp data/repos.json src/content/repos.json
```

- [ ] **Step 3: Verify fetch script compiles**

```bash
npx tsx scripts/fetch-repos.ts 2>&1 | head -3
```

Expected: Starts fetching (or completes if rate-limited). No import errors.

- [ ] **Step 4: Commit**

```bash
git add scripts/ src/content/
git commit -m "feat: add fetch script for Astro, copy existing data"
```

---

### Task 5: StarChart component

**Files:**

- Create: `src/components/StarChart.astro`

**Interfaces:**

- Props: `{ data: number[] }`

**Steps:**

- [ ] **Step 1: Create StarChart.astro**

```astro
---
interface Props {
  data: number[];
}

const { data } = Astro.props;

if (!data || data.length === 0) {
  return null;
}

const max = Math.max(...data);
const min = Math.min(...data);
const range = max - min || 1;

const bars = data.map((value, i) => {
  const height = Math.max(10, ((value - min) / range) * 100);
  const prev = i > 0 ? data[i - 1] : value;
  const color = value > prev ? 'bg-electric' : value < prev ? 'bg-red-500' : 'bg-bone/40';
  return { height, color, value };
});
---
<div class="flex items-end h-8 gap-px" aria-label="star history" role="img" aria-roledescription="sparkline chart">
  {bars.map((bar) => (
    <div
      class={`w-2 ${bar.color}`}
      style={`height: ${bar.height}%`}
      title={String(bar.value)}
    />
  ))}
</div>
```

- [ ] **Step 2: Verify component renders**

Temporarily add to `src/pages/index.astro`:

```astro
---
import Base from '../layouts/Base.astro';
import StarChart from '../components/StarChart.astro';
---
<Base title="Test">
  <StarChart data={[100, 110, 105, 120, 115]} />
</Base>
```

Run: `npx astro build`
Expected: Build succeeds, output has the sparkline divs.

- [ ] **Step 3: Commit**

```bash
git add src/components/StarChart.astro
git commit -m "feat: add StarChart component"
```

---

### Task 6: RepoCard component

**Files:**

- Create: `src/components/RepoCard.astro`

**Interfaces:**

- Props: `{ rank, full_name, description, language, url, stars, stars_gained, velocity, sparkline, slug }`

**Steps:**

- [ ] **Step 1: Create RepoCard.astro**

```astro
---
import StarChart from './StarChart.astro';

interface Props {
  rank: number;
  full_name: string;
  description: string | null;
  language: string | null;
  url: string;
  stars: number;
  stars_gained: number;
  velocity: number;
  sparkline: number[];
  slug: string;
}

const { rank, full_name, description, language, url, stars, stars_gained, velocity, sparkline, slug } = Astro.props;

const trend = stars_gained > 0 ? '↑' : stars_gained < 0 ? '↓' : '→';
const trendColor = stars_gained > 0 ? 'text-electric' : stars_gained < 0 ? 'text-red-500' : 'text-bone/40';
---
<div class="flex items-center gap-4 border-b border-bone/20 py-3 hover:bg-bone/5 transition-colors">
  <div class="w-8 text-right text-bone/50 tabular-nums">
    {String(rank).padStart(2, '0')}
  </div>
  <div class="flex-1 min-w-0">
    <a
      href={`/repo/${slug}`}
      class="text-bone hover:text-electric active:scale-[0.98] transition-transform truncate block font-bold"
    >
      {full_name}
    </a>
    <p class="text-bone/50 text-xs truncate">
      {description ?? '—'}
    </p>
    <div class="flex gap-3 text-xs mt-1 text-bone/40">
      <span>{language ?? '—'}</span>
      <span>★ {stars.toLocaleString()}</span>
      <span class={trendColor}>
        {trend} {Math.abs(stars_gained).toLocaleString()}
      </span>
    </div>
  </div>
  <div class="flex flex-col items-end">
    <StarChart data={sparkline} />
    <span class="text-xs text-electric mt-1 tabular-nums">
      {velocity} v
    </span>
  </div>
</div>
```

- [ ] **Step 2: Verify component renders**

Run: `npx astro build`
Expected: Build succeeds (component is imported by StarChart test).

- [ ] **Step 3: Commit**

```bash
git add src/components/RepoCard.astro
git commit -m "feat: add RepoCard component"
```

---

### Task 7: Header component

**Files:**

- Create: `src/components/Header.astro`

**Steps:**

- [ ] **Step 1: Create Header.astro**

```astro
---
---
<section class="min-h-[100dvh] flex flex-col justify-center">
  <header class="text-left">
    <h1 class="text-4xl font-bold tracking-tight">
      REP<span class="text-electric">Ø</span>SURGE
    </h1>
    <p class="text-bone/50 text-sm mt-2">
      repos rising. fast. ranked by star velocity.
    </p>
    <a
      href="#repos"
      class="inline-block mt-6 bg-electric text-midnight px-6 py-3 text-sm tracking-widest hover:bg-electric/80 active:scale-[0.98] transition-transform"
    >
      View Rankings
    </a>
  </header>
</section>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Header.astro
git commit -m "feat: add Header component"
```

---

### Task 8: TrustLogos component

**Files:**

- Create: `src/components/TrustLogos.astro`

**Steps:**

- [ ] **Step 1: Create TrustLogos.astro**

```astro
---
const LOGOS = [
  { slug: 'vercel', name: 'Vercel' },
  { slug: 'github', name: 'GitHub' },
  { slug: 'rust', name: 'Rust' },
  { slug: 'go', name: 'Go' },
  { slug: 'python', name: 'Python' },
  { slug: 'typescript', name: 'TypeScript' },
];
---
<section class="border-t border-bone/20 py-8">
  <div class="max-w-7xl mx-auto px-4">
    <p class="text-bone/40 text-xs tracking-widest uppercase mb-4 text-center">
      Used by developers at
    </p>
    <div class="flex flex-wrap items-center justify-center gap-8 opacity-60">
      {LOGOS.map((logo) => (
        <img
          src={`https://cdn.simpleicons.org/${logo.slug}/ffffff`}
          alt={`${logo.name} logo`}
          class="h-8 w-auto transition-opacity hover:opacity-100"
          loading="lazy"
        />
      ))}
    </div>
  </div>
</section>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/TrustLogos.astro
git commit -m "feat: add TrustLogos component"
```

---

### Task 9: FilterBar component

**Files:**

- Create: `src/components/FilterBar.astro`

**Interfaces:**

- Props: `{ period: string; language: string }`
- Exports constants: `TABS`, `LANGUAGES`

**Steps:**

- [ ] **Step 1: Create FilterBar.astro**

```astro
---
interface Props {
  period: string;
  language: string;
}

const { period, language } = Astro.props;

const TABS = [
  { key: 'day', label: 'DAY' },
  { key: 'week', label: 'WEEK' },
  { key: 'month', label: 'MONTH' },
  { key: '6m', label: '6MO' },
  { key: 'year', label: 'YEAR' },
];

const LANGUAGES = [
  { key: 'all', label: 'ALL' },
  { key: 'javascript', label: 'JS' },
  { key: 'python', label: 'PY' },
  { key: 'rust', label: 'RS' },
  { key: 'go', label: 'GO' },
  { key: 'typescript', label: 'TS' },
  { key: 'java', label: 'JV' },
];
---
<div class="flex flex-col gap-3 mb-6">
  <div class="flex flex-wrap border border-bone/30" role="tablist">
    {TABS.map((tab) => (
      <a
        href={`/?period=${tab.key}&language=${language}`}
        class={`px-3 py-2 text-xs tracking-widest border-r border-bone/30 last:border-r-0 ${
          period === tab.key
            ? 'bg-electric text-midnight'
            : 'bg-transparent text-bone hover:bg-bone/10'
        }`}
        aria-current={period === tab.key ? 'page' : undefined}
        role="tab"
        aria-selected={period === tab.key}
      >
        {tab.label}
      </a>
    ))}
  </div>
  <div class="flex flex-wrap gap-1" role="tablist">
    {LANGUAGES.map((lang) => (
      <a
        href={`/?period=${period}&language=${lang.key}`}
        class={`px-2 py-1 text-xs tracking-widest border border-bone/30 ${
          language === lang.key
            ? 'bg-electric text-midnight'
            : 'bg-transparent text-bone hover:bg-bone/10'
        }`}
        aria-current={language === lang.key ? 'page' : undefined}
        role="tab"
        aria-selected={language === lang.key}
      >
        {lang.label}
      </a>
    ))}
  </div>
</div>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/FilterBar.astro
git commit -m "feat: add FilterBar component"
```

---

### Task 10: SearchBar component

**Files:**

- Create: `src/components/SearchBar.astro`

**Steps:**

- [ ] **Step 1: Create SearchBar.astro**

```astro
---
---
<div class="mb-6">
  <input
    id="search-input"
    type="text"
    placeholder="search repos..."
    class="w-full bg-transparent border border-bone/30 px-4 py-3 text-sm text-bone placeholder:text-bone/40 focus:border-electric focus:outline-none transition-colors"
  />
</div>

<script>
  const input = document.getElementById('search-input') as HTMLInputElement;
  input?.addEventListener('input', () => {
    const query = input.value.toLowerCase();
    document.querySelectorAll('[data-repo-card]').forEach((card) => {
      const el = card as HTMLElement;
      const name = el.dataset.repoName ?? '';
      const desc = el.dataset.repoDesc ?? '';
      const lang = el.dataset.repoLang ?? '';
      const match = name.includes(query) || desc.includes(query) || lang.includes(query);
      el.style.display = match ? '' : 'none';
    });
  });
</script>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SearchBar.astro
git commit -m "feat: add SearchBar component with client filter"
```

---

### Task 11: SortSelect component

**Files:**

- Create: `src/components/SortSelect.astro`

**Steps:**

- [ ] **Step 1: Create SortSelect.astro**

```astro
---
---
<div class="mb-4 flex items-center gap-2">
  <label for="sort-select" class="text-bone/50 text-xs tracking-widest">SORT BY</label>
  <select
    id="sort-select"
    class="bg-transparent border border-bone/30 px-3 py-2 text-xs text-bone focus:border-electric focus:outline-none"
  >
    <option value="gained">Stars Gained</option>
    <option value="velocity">Velocity</option>
    <option value="total">Total Stars</option>
    <option value="name">Name</option>
  </select>
</div>

<script>
  const select = document.getElementById('sort-select') as HTMLSelectElement;
  select?.addEventListener('change', () => {
    const container = document.getElementById('repo-list');
    if (!container) return;
    const cards = Array.from(container.querySelectorAll('[data-repo-card]'));
    const key = select.value;
    cards.sort((a, b) => {
      const aEl = a as HTMLElement;
      const bEl = b as HTMLElement;
      if (key === 'name') return (aEl.dataset.repoName ?? '').localeCompare(bEl.dataset.repoName ?? '');
      const aVal = parseInt(aEl.dataset[`repoSort${key.charAt(0).toUpperCase() + key.slice(1)}`] ?? '0');
      const bVal = parseInt(bEl.dataset[`repoSort${key.charAt(0).toUpperCase() + key.slice(1)}`] ?? '0');
      return bVal - aVal;
    });
    cards.forEach((card) => container.appendChild(card));
  });
</script>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SortSelect.astro
git commit -m "feat: add SortSelect component with DOM reorder"
```

---

### Task 12: Home page

**Files:**

- Create: `src/pages/index.astro`

**Interfaces:**

- Reads: `src/content/repos.json`
- Uses: `src/lib/repo-utils.ts` (create in this task)

**Steps:**

- [ ] **Step 1: Create src/lib/repo-utils.ts**

```ts
import reposData from "../content/repos.json";

type HistoryRow = { stars: number; recorded_at: string };
type RepoRecord = {
  full_name: string;
  name: string;
  owner: string;
  description: string | null;
  language: string | null;
  url: string;
  stars: number;
  fetched_at: string;
  history: HistoryRow[];
};

export type RepoCardData = {
  rank: number;
  full_name: string;
  description: string | null;
  language: string | null;
  url: string;
  stars: number;
  stars_gained: number;
  velocity: number;
  sparkline: number[];
  slug: string;
};

const PERIOD_TO_DAYS: Record<string, number> = {
  day: 1,
  week: 7,
  month: 30,
  "6m": 180,
  year: 365,
};

export function getRepos(period: string): RepoCardData[] {
  const days = PERIOD_TO_DAYS[period] ?? 7;
  const cutoff = Date.now() - days * 86400000;
  const repos = (reposData as { repos: RepoRecord[] }).repos;

  return repos
    .map((r) => {
      const inWindow = r.history.filter(
        (h) => new Date(h.recorded_at).getTime() >= cutoff,
      );
      const baseline = inWindow.length ? inWindow[0].stars : r.stars;
      const stars_gained = r.stars - baseline;
      const sparkline = (inWindow.length ? inWindow : r.history)
        .slice(-7)
        .map((h) => h.stars);
      const velocity =
        baseline > 0
          ? Math.round((stars_gained / baseline) * 1000)
          : stars_gained;

      return {
        full_name: r.full_name,
        description: r.description,
        language: r.language,
        url: r.url,
        stars: r.stars,
        stars_gained,
        velocity,
        sparkline,
        slug: r.full_name.replace("/", "-"),
      };
    })
    .sort((a, b) => b.stars_gained - a.stars_gained)
    .map((r, i) => ({ ...r, rank: i + 1 }));
}
```

- [ ] **Step 2: Create src/pages/index.astro**

```astro
---
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import TrustLogos from '../components/TrustLogos.astro';
import FilterBar from '../components/FilterBar.astro';
import SearchBar from '../components/SearchBar.astro';
import SortSelect from '../components/SortSelect.astro';
import RepoCard from '../components/RepoCard.astro';
import { getRepos } from '../lib/repo-utils';

const period = Astro.url.searchParams.get('period') ?? 'week';
const language = Astro.url.searchParams.get('language') ?? 'all';

let repos = getRepos(period);
if (language !== 'all') {
  repos = repos.filter(
    (r) => (r.language ?? '').toLowerCase() === language
  );
}
---
<Base title="RepoSurge — repos rising fast">
  <Header />
  <TrustLogos />

  <main class="max-w-7xl mx-auto px-4" id="repos">
    <FilterBar period={period} language={language} />
    <SearchBar />
    <SortSelect />

    <div id="repo-list">
      {repos.length === 0 ? (
        <div class="py-12 text-center">
          <p class="text-bone/40 text-sm">no repos for this filter.</p>
        </div>
      ) : (
        repos.map((repo) => (
          <div
            data-repo-card
            data-repo-name={repo.full_name.toLowerCase()}
            data-repo-desc={(repo.description ?? '').toLowerCase()}
            data-repo-lang={(repo.language ?? '').toLowerCase()}
            data-repo-sort-gained={repo.stars_gained}
            data-repo-sort-velocity={repo.velocity}
            data-repo-sort-total={repo.stars}
          >
            <RepoCard
              rank={repo.rank}
              full_name={repo.full_name}
              description={repo.description}
              language={repo.language}
              url={repo.url}
              stars={repo.stars}
              stars_gained={repo.stars_gained}
              velocity={repo.velocity}
              sparkline={repo.sparkline}
              slug={repo.slug}
            />
          </div>
        ))
      )}
    </div>
  </main>
</Base>
```

- [ ] **Step 3: Verify home page builds**

```bash
npx astro build
```

Expected: Build succeeds, `dist/index.html` has hero + repo cards.

- [ ] **Step 4: Commit**

```bash
git add src/pages/index.astro src/lib/repo-utils.ts
git commit -m "feat: add home page with all components"
```

---

### Task 13: Repo detail page

**Files:**

- Create: `src/pages/repo/[slug].astro`

**Steps:**

- [ ] **Step 1: Create src/pages/repo/[slug].astro**

```astro
---
import Base from '../../layouts/Base.astro';
import StarChart from '../../components/StarChart.astro';
import { getRepos } from '../../lib/repo-utils';

export async function getStaticPaths() {
  const repos = getRepos('year');
  return repos.map((repo) => ({
    params: { slug: repo.slug },
    props: { repo },
  }));
}

const { repo } = Astro.props;
---
<Base title={`${repo.full_name} — RepoSurge`}>
  <main class="max-w-7xl mx-auto px-4 py-16">
    <a href="/" class="text-bone/50 text-sm hover:text-electric transition-colors mb-8 inline-block">
      ← Back to Rankings
    </a>

    <div class="border border-bone/20 p-8">
      <div class="flex items-start justify-between mb-8">
        <div>
          <h1 class="text-3xl font-bold tracking-tight mb-2">
            {repo.full_name}
          </h1>
          <p class="text-bone/50 text-sm">
            {repo.description ?? '—'}
          </p>
        </div>
        <a
          href={repo.url}
          target="_blank"
          rel="noreferrer"
          class="bg-electric text-midnight px-4 py-2 text-xs tracking-widest hover:bg-electric/80 transition-colors"
        >
          View on GitHub
        </a>
      </div>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-8">
        <div>
          <p class="text-bone/40 text-xs tracking-widest mb-1">TOTAL STARS</p>
          <p class="text-2xl font-bold tabular-nums">{repo.stars.toLocaleString()}</p>
        </div>
        <div>
          <p class="text-bone/40 text-xs tracking-widest mb-1">STARS GAINED</p>
          <p class={`text-2xl font-bold tabular-nums ${repo.stars_gained > 0 ? 'text-electric' : repo.stars_gained < 0 ? 'text-red-500' : 'text-bone/40'}`}>
            {repo.stars_gained > 0 ? '+' : ''}{repo.stars_gained.toLocaleString()}
          </p>
        </div>
        <div>
          <p class="text-bone/40 text-xs tracking-widest mb-1">VELOCITY</p>
          <p class="text-2xl font-bold tabular-nums text-electric">{repo.velocity} v</p>
        </div>
        <div>
          <p class="text-bone/40 text-xs tracking-widest mb-1">LANGUAGE</p>
          <p class="text-2xl font-bold">{repo.language ?? '—'}</p>
        </div>
      </div>

      <div>
        <p class="text-bone/40 text-xs tracking-widest mb-3">STAR HISTORY (7 DAYS)</p>
        <StarChart data={repo.sparkline} />
      </div>
    </div>
  </main>
</Base>
```

- [ ] **Step 2: Verify detail page builds**

```bash
npx astro build
```

Expected: Build succeeds, `dist/repo/facebook-react/index.html` exists.

- [ ] **Step 3: Commit**

```bash
git add src/pages/repo/
git commit -m "feat: add repo detail page"
```

---

### Task 14: About page

**Files:**

- Create: `src/pages/about.astro`

**Steps:**

- [ ] **Step 1: Create about.astro**

```astro
---
import Base from '../layouts/Base.astro';
---
<Base title="About — RepoSurge">
  <main class="max-w-3xl mx-auto px-4 py-16">
    <h1 class="text-3xl font-bold tracking-tight mb-8">About RepoSurge</h1>

    <div class="space-y-8 text-bone/70 text-sm leading-relaxed">
      <section>
        <h2 class="text-electric text-xs tracking-widest uppercase mb-3">What is this?</h2>
        <p>
          RepoSurge tracks the fastest-rising open source projects on GitHub.
          We measure star velocity — how quickly a repo gains stars relative to
          its existing count — to surface projects gaining momentum, not just
          the ones with the most stars.
        </p>
      </section>

      <section>
        <h2 class="text-electric text-xs tracking-widest uppercase mb-3">How it works</h2>
        <p>
          Every day, we fetch the top 100 repos across 6 languages (JavaScript,
          Python, Rust, Go, TypeScript, Java) sorted by stars. We compare today's
          count against a historical baseline to calculate velocity.
        </p>
        <p class="mt-3">
          Velocity = (stars gained / baseline stars) × 1000. A velocity of 5 means
          the repo gained 0.5% of its star count in the measurement period.
        </p>
      </section>

      <section>
        <h2 class="text-electric text-xs tracking-widest uppercase mb-3">Data source</h2>
        <p>
          All data comes from the GitHub Search API. Star counts are snapshots,
          not real-time. History is accumulated over daily fetches.
        </p>
      </section>

      <section>
        <h2 class="text-electric text-xs tracking-widest uppercase mb-3">Stack</h2>
        <p>
          Astro, Tailwind CSS, vanilla JavaScript. No frameworks, no
          dependencies, no JavaScript shipped to the client unless it handles
          search or sort.
        </p>
      </section>
    </div>
  </main>
</Base>
```

- [ ] **Step 2: Verify about page builds**

```bash
npx astro build
```

Expected: Build succeeds, `dist/about/index.html` exists.

- [ ] **Step 3: Commit**

```bash
git add src/pages/about.astro
git commit -m "feat: add about page"
```

---

### Task 15: Local preferences

**Files:**

- Modify: `src/pages/index.astro`

**Steps:**

- [ ] **Step 1: Add localStorage script to index.astro**

Add before closing `</Base>`:

```astro
<script>
  // Restore last used period/language from localStorage
  const stored = localStorage.getItem('reposurge-prefs');
  if (stored) {
    try {
      const prefs = JSON.parse(stored);
      const params = new URLSearchParams(window.location.search);
      if (!params.has('period') && prefs.period) {
        params.set('period', prefs.period);
      }
      if (!params.has('language') && prefs.language) {
        params.set('language', prefs.language);
      }
      if (params.toString() !== window.location.search.slice(1)) {
        window.location.search = params.toString();
      }
    } catch {}
  }

  // Save current period/language to localStorage on change
  document.querySelectorAll('a[href*="period="]').forEach((link) => {
    link.addEventListener('click', () => {
      const url = new URL((link as HTMLAnchorElement).href);
      const period = url.searchParams.get('period');
      const language = url.searchParams.get('language');
      if (period || language) {
        localStorage.setItem('reposurge-prefs', JSON.stringify({
          period: period ?? 'week',
          language: language ?? 'all',
        }));
      }
    });
  });
</script>
```

- [ ] **Step 2: Verify build still passes**

```bash
npx astro build
```

Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro
git commit -m "feat: add localStorage preferences for period/language"
```

---

### Task 16: Build and verify

**Files:** None (verification only)

**Steps:**

- [ ] **Step 1: Full build**

```bash
npx astro build
```

Expected: 0 errors, all pages generated.

- [ ] **Step 2: Verify output structure**

```bash
ls -la dist/
ls -la dist/repo/ | head -10
```

Expected: `dist/index.html`, `dist/about/index.html`, `dist/repo/*/index.html`.

- [ ] **Step 3: Preview**

```bash
npx astro preview &
sleep 2
curl -s http://localhost:4321/ | head -20
kill %1
```

Expected: HTML response with nav + hero.

- [ ] **Step 4: Cleanup old files**

```bash
rm -rf app/ components/ lib/ src/atoms/ src/molecules/ src/features/ src/organisms/ src/types/ src/lib/services/
rm -f next-env.d.ts next.config.js postcss.config.mjs tailwind.config.ts tsconfig.json
rm -rf .next/
```

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore: remove old Next.js files, final Astro build verified"
```
